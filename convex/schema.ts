import { defineSchema } from "convex/server";

// No tables yet — schema will be defined in a later phase.
// This file exists so `npx convex dev` and `npx convex codegen` have a valid entry point.
export default defineSchema({});
